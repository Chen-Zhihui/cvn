#include <Cvn/Caffe2util/CPredictor.h>
#include <Poco/Path.h>


bool CPredictor::load(const std::string & jsonstr) {
    // json -> config Poco::Path path(dir);
    // load net for init net, predict net
    // init net
    // create predictor
    return true;
}